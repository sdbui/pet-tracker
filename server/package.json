{
  "name": "server",
  "version": "1.0.0",
  "description": "backend for pet tracker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf db2.sqlite && rm -rf ./public/uploads && rm -rf dbtest.sqlite",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "md5": "^2.3.0",
    "multer": "^1.4.5-lts.1",
    "sequelize": "^6.30.0",
    "sqlite3": "^5.1.2"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.0"
  }
}
